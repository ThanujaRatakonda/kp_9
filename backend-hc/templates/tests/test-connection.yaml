apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-hc.fullname" . }}-test-connection"
  labels:
    {{- include "backend-hc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-hc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
