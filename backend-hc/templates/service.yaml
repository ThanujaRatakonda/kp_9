apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend-hc.fullname" . }}
  labels:
    {{- include "backend-hc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backend-hc.selectorLabels" . | nindent 4 }}
