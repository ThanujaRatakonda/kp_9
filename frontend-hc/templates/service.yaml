apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-hc.fullname" . }}
  labels:
    {{- include "frontend-hc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontend-hc.selectorLabels" . | nindent 4 }}
