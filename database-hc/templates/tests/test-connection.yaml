apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "database-hc.fullname" . }}-test-connection"
  labels:
    {{- include "database-hc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "database-hc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
