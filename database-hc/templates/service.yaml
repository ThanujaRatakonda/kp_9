apiVersion: v1
kind: Service
metadata:
  name: {{ include "database-hc.fullname" . }}
  labels:
    {{- include "database-hc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "database-hc.selectorLabels" . | nindent 4 }}
